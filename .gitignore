# Per-user identity (persisted SSO email, never commit)
data/.user_identity

# Environment and secrets
.env
*.nev
.env.*
!.env.example
secrets.*
credentials.*
*_key.txt
*_token.txt

# Python virtual environment
.venv/
venv/
env/

# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/

# macOS files
.DS_Store
.AppleDouble
.LSOverride

# Backup and temporary files
*.bak
*.tmp
*~
*.swp
*.swo

# Logs
*.log
streamlit*.log

# Generated/temporary data files
*.csv
!requirements.txt
!runtime.txt

# Ignore repo artifacts and large outputs
y/
*.parquet
*.joblib
*.sqlite

# Allow scan artifacts for auto-scan sync
!data/scans/
data/scans/*
!data/scans/latest_scan.parquet
!data/scans/latest_scan.json
!data/scans/latest_scan.meta.json

# Ignore live scan files (local only)
data/scans/latest_scan_live.*
data/latest_scan_live.*

# Fundamentals SQLite store
data/fundamentals_store.sqlite
data/*.sqlite

# DuckDB — Supabase is the primary DB; DuckDB is local-only fallback
data/*.duckdb
data/*.duckdb.wal

# Model artifacts (keep only production model)
model_5d.pkl
model_5d_full.pkl
model_logistic_5d.pkl
calibration_curve.csv
feature_importance.csv
shap_values*.csv

# Old archives and historical data
archive/
archive_*/
old_*/
backup_*/

# Tools debug output (keep tools scripts, ignore output)
tools/debug/*.csv
tools/debug/*.log
tools/benchmarks/*.csv
tools/benchmarks/*.log
tools/audit/*.csv
tools/audit/*.log

# IDE files
.vscode/
.idea/
*.code-workspace

# ML artifacts (ignore all except production bundle metadata)
ml/*
!ml/bundles/
ml/bundles/*
!ml/bundles/latest/
ml/bundles/latest/*
!ml/bundles/latest/metadata.json
!ml/bundles/latest/model.joblib
models/*.joblib
.claude/
