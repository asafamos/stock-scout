# StockScout — facegreet migration credentials
# ----------------------------------------------
# 1. Copy this file:    cp .env.facegreet-migration.template .env.facegreet-migration
# 2. Fill in real values below (replace each ... with actual secret)
# 3. Load all at once:  set -a; source .env.facegreet-migration; set +a
# 4. Verify:            .venv/bin/python scripts/check_env_ascii.py
# 5. Migrate:           .venv/bin/python scripts/migrate_supabase_to_r2.py --dry-run --limit 5
#
# IMPORTANT: the real file (.env.facegreet-migration without .template) is
# gitignored — never commit it. After migration, delete it.

SUPABASE_URL=https://ewkrpukgyqndmznituzg.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_REPLACE_ME_WITH_ACTUAL_VALUE
SUPABASE_BUCKET=facegreet-videos

R2_ACCOUNT_ID=91006435fd2340fdf98a522be868b198
R2_ACCESS_KEY=REPLACE_ME_with_R2_API_Token_Access_Key_ID
R2_SECRET_KEY=REPLACE_ME_with_R2_API_Token_Secret_Access_Key
R2_BUCKET=facegreet-videos
